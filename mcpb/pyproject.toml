[project]
name = "palinex-mcpb"
version = "0.3.0"
description = "Palinex packaged as a Claude Desktop .mcpb extension."
requires-python = ">=3.12"
dependencies = [
    "palinex[mcp,nexus]>=0.3.0",
]

[tool.uv]
package = false
